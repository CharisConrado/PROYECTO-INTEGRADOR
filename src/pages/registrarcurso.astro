---

---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link
			rel="icon"
			href="/logo2.png"
		/>
		<meta name="generator" content={Astro.generator} />
		<title>Cambiar contraseña</title>
</head>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.querySelector('form');
    const modal = document.getElementById('modal');
    const closeModalButton = document.getElementById('close-modal');

    function handleSubmit(e) {
      e.preventDefault(); // Prevenir el envío del formulario

      // Mostrar el modal
      modal.style.display = 'block';
    }

    // Añadir listener para el envío del formulario
    form.addEventListener('submit', handleSubmit);

    // Cerrar el modal y limpiar el formulario cuando se presiona el botón de cerrar
    closeModalButton.addEventListener('click', () => {
      modal.style.display = 'none';

      // Limpiar el formulario después de cerrar el modal
      form.reset();
    });
  });
</script>

<html title="Registro al Curso">
    <body class="bg-cover bg-center min-h-screen" style="background-image: url('https://imagenes.eltiempo.com/files/image_1200_600/uploads/2021/09/25/614f7e2a4c436.jpeg');">

        <div class="flex items-center justify-center min-h-screen bg-black bg-opacity-60">
          
          <!-- Ajuste en el tamaño del contenedor para que el cuadro esté centrado y no ocupe toda la pantalla -->
          <div class="w-full max-w-md mx-auto backdrop-blur-3xl p-8 md:p-12 rounded-lg shadow-lg flex flex-col items-center bg-opacity-0">
            
            <div class="mt-[13px]">
                <img src="/public/Logo.jpeg" alt="Logo" class="w-10 h-10 rounded-full">
            </div>
      
            <h2 class="text-[#4F774D] text-3xl font-bold">Registro al curso</h2>
      
            <form>
                
                
                <!-- Nombres -->
                <label for="nombres" class="block text-[#4F774D] font-semibold mb-1" >Nombres:</label>
                <input type="text" id="nombres" name="nombres" required class="w-full p-3 rounded-lg border border-[#D9D9D9] focus:outline-none focus:border-[#8AB6D6]">
                
                <!-- Apellidos -->
                <label for="apellidos" class="block text-[#4F774D] font-semibold">Apellidos:</label>
                <input type="text" id="apellidos" name="apellidos" required class="w-full p-3 rounded-lg border border-[#D9D9D9] focus:outline-none focus:border-[#8AB6D6]">
                
                <!-- Nivel de educación -->
                <label for="educacion" class="block text-[#4F774D] font-semibold">Nivel de educación:</label>
                <select id="educacion" name="educacion" required class="w-full p-3 rounded-lg border border-[#D9D9D9] focus:outline-none focus:border-[#8AB6D6]">
                  <option value="preescolar">Educación preescolar</option>
                  <option value="primaria">Educación básica (primaria y secundaria)</option>
                  <option value="media">Educación media (bachiller)</option>
                  <option value="superior">Educación superior</option>
                </select>
        
                <!-- Subir certificado -->
                <label for="certificado" class="block                                            text-[#4F774D] font-semibold">Subir certificado (PDF):</label>
                <input type="file" id="certificado" name="certificado" accept=".pdf" required class="w-full p-2 mt-1 mb-4 border border-[#A16436] rounded-lg focus:outline-none focus:ring-2 focus:ring-[#94C77E]">
                
                <!-- Botón de enviar -->
                <button class="w-full py-3 rounded-lg bg-[#F4A261] text-white font-semibold hover:bg-[#A16436] transition duration-200">
                    <a href="/clave">Enviar datos</a>
                </button>
              </form>
            
            
            
          </div>
          
        </div>
      
      </body>
</html>
